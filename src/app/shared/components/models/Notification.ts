/*
export class Notification {
  id: number;
  content: string;
  timestamp: Date;
  recipientUserId: number;
  senderUserId: number;

  constructor(id: number, content: string, timestamp: Date, recipientUserId: number, senderUserId: number) {
    this.id = id;
    this.content = content;
    this.timestamp = timestamp;
    this.recipientUserId = recipientUserId;
    this.senderUserId = senderUserId;
  }

  getMessage(): string {
    // Logique pour obtenir le message à partir des autres propriétés
    return this.content; // ou toute autre logique nécessaire
  }
}
*/
